#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

main()
{
	setlocale(LC_ALL, "Portuguese");

	// definição de variáveis

	float price_chopp = 18.00, price_pizzaG = 60.00, price_cobert = 15.00, price_refri360ml = 12.00, price_water = 10.00, value_pessoa = 0, total = 0, porC_gorjeta = 0.08, value_consum = 0;
	int qtd_pess, qtd_chopp, qtd_pizza, qtd_cobertura, qtd_refri, qtd_water;

	char pizzariaName[50] = "????????????????";

	return 0;
}
